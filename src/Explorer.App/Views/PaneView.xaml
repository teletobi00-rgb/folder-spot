<UserControl x:Class="Explorer.App.Views.PaneView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewmodels="clr-namespace:Explorer.App.ViewModels"
             xmlns:views="clr-namespace:Explorer.App.Views"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewmodels:PaneViewModel}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 탭 스트립 -->
        <Grid Grid.Row="0" Margin="0,0,0,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <ListBox x:Name="TabStrip"
                     Grid.Column="0"
                     AllowDrop="True"
                     ItemsSource="{Binding Tabs}"
                     SelectedItem="{Binding ActiveTab}"
                     ScrollViewer.HorizontalScrollBarVisibility="Auto"
                     ScrollViewer.VerticalScrollBarVisibility="Disabled"
                     PreviewMouseLeftButtonDown="OnTabStripPreviewMouseLeftButtonDown"
                     PreviewMouseMove="OnTabStripPreviewMouseMove"
                     DragOver="OnTabStripDragOver"
                     Drop="OnTabStripDrop">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemContainerStyle>
                    <!-- 탭 폭/높이 축소: 테마 템플릿 유지하고 패딩만 줄인다(BasedOn 없이 세터만). -->
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="8,3" />
                        <Setter Property="Margin" Value="0" />
                        <Setter Property="MinHeight" Value="0" />
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" ToolTip="{Binding Path}">
                            <TextBlock MaxWidth="88"
                                       VerticalAlignment="Center"
                                       FontSize="12"
                                       Text="{Binding Title}"
                                       TextTrimming="CharacterEllipsis" />
                            <Button Margin="4,0,0,0"
                                    Padding="3,0"
                                    VerticalAlignment="Center"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Command="{Binding DataContext.CloseTabCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                    CommandParameter="{Binding}"
                                    Content="✕"
                                    FontSize="9"
                                    ToolTip="탭 닫기 (Ctrl+W)" />
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <ui:Button Grid.Column="1"
                       Margin="4,0,0,0"
                       Padding="8,2"
                       Command="{Binding AddTabCommand}"
                       Content="+"
                       ToolTip="새 탭 (Ctrl+T)" />
        </Grid>

        <Grid Grid.Row="1">
            <!-- DataContext를 자식 VM으로 바꾸면 같은 엘리먼트의 Visibility 바인딩도 자식 VM 기준이 된다.
                 IsPreviewMode는 PaneViewModel 소유이므로 RelativeSource로 페인 VM을 직접 가리킨다. -->
            <views:FileListView x:Name="FileArea"
                                DataContext="{Binding FileList}"
                                Visibility="{Binding DataContext.IsPreviewMode, RelativeSource={RelativeSource AncestorType=views:PaneView}, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />
            <views:PreviewView DataContext="{Binding Preview}"
                               Visibility="{Binding DataContext.IsPreviewMode, RelativeSource={RelativeSource AncestorType=views:PaneView}, Converter={StaticResource BooleanToVisibilityConverter}}" />
        </Grid>
    </Grid>
</UserControl>
