<UserControl x:Class="Explorer.App.Views.PaneView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewmodels="clr-namespace:Explorer.App.ViewModels"
             xmlns:views="clr-namespace:Explorer.App.Views"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewmodels:PaneViewModel}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 탭 스트립 -->
        <Grid Grid.Row="0" Margin="0,0,0,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <ListBox x:Name="TabStrip"
                     Grid.Column="0"
                     AllowDrop="True"
                     ItemsSource="{Binding Tabs}"
                     SelectedItem="{Binding ActiveTab}"
                     ScrollViewer.HorizontalScrollBarVisibility="Auto"
                     ScrollViewer.VerticalScrollBarVisibility="Disabled"
                     PreviewMouseLeftButtonDown="OnTabStripPreviewMouseLeftButtonDown"
                     PreviewMouseMove="OnTabStripPreviewMouseMove"
                     DragOver="OnTabStripDragOver"
                     Drop="OnTabStripDrop">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" ToolTip="{Binding Path}">
                            <TextBlock MaxWidth="140"
                                       VerticalAlignment="Center"
                                       Text="{Binding Title}"
                                       TextTrimming="CharacterEllipsis" />
                            <Button Margin="6,0,0,0"
                                    Padding="4,0"
                                    VerticalAlignment="Center"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Command="{Binding DataContext.CloseTabCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                    CommandParameter="{Binding}"
                                    Content="✕"
                                    FontSize="10"
                                    ToolTip="탭 닫기 (Ctrl+W)" />
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <ui:Button Grid.Column="1"
                       Margin="4,0,0,0"
                       Padding="8,2"
                       Command="{Binding AddTabCommand}"
                       Content="+"
                       ToolTip="새 탭 (Ctrl+T)" />
        </Grid>

        <Grid Grid.Row="1">
            <views:FileListView x:Name="FileArea"
                                DataContext="{Binding FileList}"
                                Visibility="{Binding IsPreviewMode, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />
            <views:PreviewView DataContext="{Binding Preview}"
                               Visibility="{Binding IsPreviewMode, Converter={StaticResource BooleanToVisibilityConverter}}" />
        </Grid>
    </Grid>
</UserControl>
