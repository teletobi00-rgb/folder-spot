<ui:FluentWindow x:Class="Explorer.App.Views.WhatsNewWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 Title="새 기능"
                 Width="500"
                 Height="460"
                 FontFamily="{StaticResource AppFontFamily}"
                 FontSize="13"
                 ExtendsContentIntoTitleBar="True"
                 ResizeMode="NoResize"
                 WindowBackdropType="Mica"
                 WindowCornerPreference="Round"
                 WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="새 기능" />

        <ScrollViewer Grid.Row="1" Margin="20,8,20,0" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock x:Name="HeaderText"
                           FontSize="17"
                           FontWeight="SemiBold"
                           TextWrapping="Wrap" />
                <TextBlock Margin="0,2,0,12"
                           Opacity="0.6"
                           Text="이번 업데이트에서 달라진 점입니다." />
                <ItemsControl x:Name="NotesList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Margin="0,0,8,0"
                                           Foreground="{DynamicResource AccentTextFillColorPrimaryBrush}"
                                           Text="•" />
                                <TextBlock Grid.Column="1" Text="{Binding}" TextWrapping="Wrap" />
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="2"
                    Margin="20,12,20,16"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal">
            <ui:Button MinWidth="80"
                       Appearance="Primary"
                       Click="OnCloseClick"
                       Content="확인" />
        </StackPanel>
    </Grid>
</ui:FluentWindow>
